{
  "name": "playwright-cucumber-framework",
  "version": "1.1.0",
  "scripts": {
    "test": "cucumber-js",
    "test:env:dev": "ENV=dev cucumber-js",
    "test:env:qa": "ENV=qa cucumber-js",
    "report": "allure generate allure-results --clean -o allure-report && allure open"
  },
  "devDependencies": {
    "@cucumber/cucumber": "^9.0.0",
    "playwright": "^1.41.0",
    "allure-commandline": "^2.20.1",
    "@playwright/test": "^1.41.0"

  }
}
